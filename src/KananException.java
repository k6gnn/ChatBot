public class KananException extends Exception { //this works as fine, makes it related to the chatbot instead of whole java 
    public KananException(String message) {
        super(message);
    }
}
